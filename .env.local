
# Copy to .env.local and fill with your MySQL credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=school_db
DB_PORT=3306
# If deploying to serverless (e.g., Vercel), local image uploads won't persist. Use a persistent host.
